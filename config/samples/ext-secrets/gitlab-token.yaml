apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-token
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: gitlab-token
  data:
    - secretKey: token
      remoteRef:
        key: gitlab-pat
