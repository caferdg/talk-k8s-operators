---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: gitlabprojects.platform.caferdg.io
spec:
  group: platform.caferdg.io
  names:
    kind: GitlabProject
    listKind: GitlabProjectList
    plural: gitlabprojects
    shortNames:
    - gproject
    singular: gitlabproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.fullPath
      name: Full Path
      type: string
    - jsonPath: .spec.trackedBranch
      name: Branch
      type: string
    - jsonPath: .status.lastCommit.sha
      name: Last Commit
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitlabProject is the Schema for the gitlabprojects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitlabProjectSpec defines the desired state of GitlabProject.
            properties:
              notifierRef:
                description: Reference to a Notifier CR name in the same namespace
                type: string
              parentGroupRef:
                description: Reference to a GitlabGroup CR name in the same namespace
                type: string
              properties:
                description: GitLab project properties
                properties:
                  description:
                    description: Description of the project
                    type: string
                  name:
                    description: Name of the project
                    type: string
                  visibility:
                    description: Visibility level (private, internal, public)
                    type: string
                required:
                - name
                type: object
              trackedBranch:
                description: Branch to track
                type: string
            required:
            - parentGroupRef
            - properties
            - trackedBranch
            type: object
          status:
            description: GitlabProjectStatus defines the observed state of GitlabProject.
            properties:
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              description:
                description: Description of the project
                type: string
              fullPath:
                description: Full path of the project on GitLab
                type: string
              lastCommit:
                description: Last commit on the tracked branch
                properties:
                  author:
                    description: Author name
                    type: string
                  createdAt:
                    description: Commit timestamp
                    type: string
                  message:
                    description: Commit message
                    type: string
                  sha:
                    description: Commit SHA
                    type: string
                type: object
              lastPipeline:
                description: Last pipeline on the tracked branch
                properties:
                  createdAt:
                    description: Pipeline creation timestamp
                    type: string
                  id:
                    description: Pipeline ID
                    type: integer
                  status:
                    description: Pipeline status (running, success, failed, etc.)
                    type: string
                  webUrl:
                    description: Pipeline web URL
                    type: string
                type: object
              stamped:
                description: Whether the project has been stamped on GitLab
                type: boolean
              visibility:
                description: Visibility level (private, internal, public)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
